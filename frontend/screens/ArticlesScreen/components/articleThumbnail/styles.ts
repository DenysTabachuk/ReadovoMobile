import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  image: {
    borderRadius: 6,
    height: 88,
    width: 88,
  },
  placeholder: {
    alignItems: 'center',
    borderRadius: 6,
    borderWidth: 1,
    height: 88,
    justifyContent: 'center',
    width: 88,
  },
});
